    - name: "Install YUM packages"
      yum:
        name: "{{ item }}"
        state: present
        enablerepo: epel
      with_items:
        "{{ yum_packages }}"
      environment:
        RUNLEVEL: 1
      when: ( ansible_pkg_mgr == "yum" )
