    - name: "Install APT packages"
      apt: name="{{ item }}" state=present
      with_items:
        "{{ apt_packages }}"
      environment:
        RUNLEVEL: 1
      when: ( ansible_pkg_mgr == "apt" )
